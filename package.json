{
  "name": "mini-types",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish --exact",
    "release": "sh ./scripts/set-release-tag.sh"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
