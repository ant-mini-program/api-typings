{
  "name": "mini-types",
  "private": true,
  "scripts": {
    "publish": "lerna publish --exact --force-publish='*'",
    "release": "sh ./scripts/set-release-tag.sh"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
